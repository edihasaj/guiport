import Foundation

public enum Recorder {
    public static func record(target: AppTarget, to path: String) throws {
        throw GuiportError(code: "not_implemented", message: "record not implemented yet — write tests by hand for now")
    }
}
